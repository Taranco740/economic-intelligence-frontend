"use client";
import {useEffect,useMemo,useRef,useState} from "react";
import {createSupabaseBrowserClient} from "../../lib/supabase-browser";
import {createProject,getProjects,uploadDataset,runIntelligence,getHistory,createHistory,renameHistory,deleteHistory,askData,type Project,type Dataset,type IntelligenceResult,type HistoryItem} from "../../lib/api";
import ArtifactViews from "./artifact-views";
import ForecastView from "./forecast-view";

type Tab="analyze"|"visualize"|"dashboard"|"forecast"|"report";
const tabs:[Tab,string][]=[["analyze","Analytics"],["visualize","Visualize + Statistics"],["dashboard","Dashboard"],["forecast","Forecast"],["report","Report"]];
const askLabels:Record<Tab,string>={analyze:"Ask Analytics",visualize:"Ask Visuals + Statistics",dashboard:"Ask Dashboard",forecast:"Ask Forecast",report:"Ask Report"};
function Markdown({text}:{text:string}){return <div className="markdown">{text.split(/\r?\n/).map((line,i)=>{const s=line.trim();if(!s)return <div key={i} style={{height:6}}/>;const m=s.match(/^#{1,4}\s+(.*)$/);if(m)return <p key={i}><strong>{m[1]}</strong></p>;if(/^[-*]\s+/.test(s))return <p key={i}>• {s.replace(/^[-*]\s+/,"")}</p>;return <p key={i}>{s}</p>})}</div>}
export default function Workspace(){
 const [tab,setTab]=useState<Tab>("analyze");const [projects,setProjects]=useState<Project[]>([]);const [project,setProject]=useState<Project|null>(null);const [dataset,setDataset]=useState<Dataset|null>(null);const [result,setResult]=useState<IntelligenceResult|null>(null);const [history,setHistory]=useState<HistoryItem[]>([]);const [prompt,setPrompt]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [selected,setSelected]=useState<any>(null);const [chat,setChat]=useState<{role:string;text:string}[]>([]);const [profileOpen,setProfileOpen]=useState(false);const [renameId,setRenameId]=useState<string|null>(null);const [renameValue,setRenameValue]=useState("");const [deleteId,setDeleteId]=useState<string|null>(null);const inputRef=useRef<HTMLInputElement>(null);const sb=createSupabaseBrowserClient();
 useEffect(()=>{if(!sb)return;void sb.auth.getSession().then(async({data})=>{if(!data.session)return;try{const[p,h]=await Promise.all([getProjects(data.session.access_token),getHistory(data.session.access_token)]);setProjects(p);setHistory(h);if(p[0])setProject(p[0])}catch(e){setError(e instanceof Error?e.message:"Could not load Gamur.")}})},[sb]);
 const token=async()=>{const s=await sb?.auth.getSession();return s?.data.session?.access_token||""};
 const context=useMemo(()=>result?JSON.stringify({workspace:tab,dataset:dataset?.name,analysis:result.analysis,visualization:result.visualization,questions:result.questions,insights:result.insights,forecasting:result.forecasting}):"",[result,dataset,tab]);
 const run=async(file?:File)=>{setBusy(true);setError("");try{const t=await token();if(!t)throw Error("Please sign in first.");let p=project;if(!p){p=await createProject(t,file?.name.replace(/\.(csv|xlsx)$/i,"")||"My analysis");setProject(p);setProjects(x=>[p!,...x])}let d=dataset;if(file)d=await uploadDataset(t,p.id,file.name.replace(/\.(csv|xlsx)$/i,""),file);if(!d)throw Error("Upload a CSV or XLSX dataset first.");setDataset(d);const r=await runIntelligence(t,p.id,d.id,prompt.trim()||"Automatically investigate this dataset comprehensively. Infer the likely domain and purpose. Clean and profile the data, identify domain-specific signals, anomalies, relationships, trends, risks, opportunities and data-quality gaps, answer useful questions with computed evidence, determine forecastability, and prepare reusable evidence for visualization plus statistics, dashboard, forecast and report.",["cleaning","analysis","forecasting","insights","visualization","report"]);setResult(r);const h=await createHistory(t,p.id,d.name||"Analysis","full",d.id,r);setHistory(x=>[h,...x.filter(i=>i.id!==h.id)]);setPrompt("");setTab("analyze")}catch(e){setError(e instanceof Error?e.message:"Gamur could not complete the analysis.")}finally{setBusy(false)}};
 const openHistory=(h:HistoryItem)=>{setError("");setProject(projects.find(p=>p.id===h.project_id)||null);setDataset(h.dataset_id?{id:h.dataset_id,project_id:h.project_id,name:h.title||"Saved analysis",source_type:"",description:null,current_version:0,created_at:h.created_at,updated_at:h.created_at}:null);setResult(h.result&&Object.keys(h.result).length?h.result:null);setChat([]);setTab("analyze");if(!h.result||!Object.keys(h.result).length)setError("This older analysis has no saved snapshot. Re-run it once to create one.")};
 const sendChat=async()=>{const q=prompt.trim();if(!q||busy)return;setPrompt("");setChat(x=>[...x,{role:"user",text:q}]);try{const r=await askData(await token(),`${askLabels[tab]}: ${q}`,context);setChat(x=>[...x,{role:"assistant",text:r.answer}])}catch(e){setChat(x=>[...x,{role:"assistant",text:e instanceof Error?e.message:"Assistant unavailable."}])}};
 const deleteSaved=async()=>{if(!deleteId)return;try{await deleteHistory(await token(),deleteId);setHistory(x=>x.filter(h=>h.id!==deleteId));setDeleteId(null)}catch(e){setError(e instanceof Error?e.message:"Could not delete analysis.")}};
 const renameSaved=async()=>{if(!renameId)return;try{const h=await renameHistory(await token(),renameId,renameValue.trim()||"Analysis");setHistory(x=>x.map(i=>i.id===h.id?h:i))}catch(e){setError(e instanceof Error?e.message:"Could not rename analysis.")}setRenameId(null)};
 const a=result?.analysis;const rels=a?.relationships||result?.visualization?.relationships||[];const questions=result?.questions||[];const outliers=a?.outliers||[];
 let body:any;
 if(!result){body=<section className="landing"><div className="landingBadge">✦ AUTOMATIC INVESTIGATION</div><h2>Upload your data.<br/><em>Gamur answers.</em></h2><p>Bring in a CSV or XLSX and Gamur automatically cleans, profiles and investigates it. When analysis is complete, every workspace reuses the same evidence.</p><button className="dropzone" onClick={()=>inputRef.current?.click()} disabled={busy}><span className="uploadIcon">↑</span><strong>{busy?"Investigating…":"Attach your data"}</strong><small>CSV or XLSX · automatic investigation</small></button><div className="featureGrid"><div><b>01</b><strong>Signals</strong><span>What matters most</span></div><div><b>02</b><strong>Anomalies</strong><span>What needs attention</span></div><div><b>03</b><strong>Relationships</strong><span>What moves together</span></div><div><b>04</b><strong>Questions</strong><span>What the data can answer</span></div></div></section>}
 else if(tab==="analyze"){body=<div className="results"><section className="readout"><div className="readoutMain"><span className="sectionTag">ANALYTICS</span><h2>What is happening?</h2><Markdown text={result.insights||"Gamur found measurable patterns in the completed analysis."}/><div className="evidenceLine"><span>Evidence-led</span><span>{a?.numeric_columns?.length||0} numeric fields</span><span>{a?.target?`Target: ${a.target}`:"Descriptive analysis"}</span></div></div><div className="readoutSide"><span>INVESTIGATION</span><b>Complete</b><small>Evidence is reusable across all workspaces</small></div></section><div className="signalGrid"><div className="signal"><span>STRONGEST SIGNAL</span><b>{rels[0]?.x||"—"}</b><strong>{rels[0]?`r = ${Number(rels[0].r).toFixed(2)}`:"—"}</strong><small>{a?.target?`relationship with ${a.target}`:"Primary relationship"}</small></div><div className="signal"><span>OBSERVATIONS</span><b>{a?.rows?.toLocaleString()||0}</b><strong>{a?.columns||0} fields</strong><small>after preparation</small></div><div className="signal"><span>ANOMALIES</span><b>{outliers.reduce((n,x)=>n+x.count,0)}</b><strong>outlier flags</strong><small>IQR screening</small></div><div className="signal"><span>QUESTIONS</span><b>{questions.length}</b><strong>answered</strong><small>evidence-supported</small></div></div><section className="section"><div className="sectionHead"><div><span className="sectionTag">DISCOVERIES</span><h2>What Gamur found</h2></div><span className="muted">Computed evidence, not generic text</span></div><div className="discoveryGrid">{rels.slice(0,8).map((r:any,i:number)=><button className="discovery" key={i} onClick={()=>setSelected(r)}><div className="discoveryTop"><span className="pill">{r.strength||"Association"}</span><b>r {Number(r.r).toFixed(2)}</b></div><h3>{r.x} <i>↔</i> {r.y}</h3><p>Measured relationship in the observed data. Inspect the evidence before interpreting it.</p><span className="explore">Inspect evidence ↗</span></button>)}</div></section><section className="section"><div className="sectionHead"><div><span className="sectionTag">QUESTIONS</span><h2>Questions your data can answer</h2></div></div><div className="questionGrid">{questions.map((q:any,i:number)=><button className="question" key={i} onClick={()=>setSelected(q)}><span className="qNum">{String(i+1).padStart(2,"0")}</span><div><b>{q.question}</b><small>{q.answer}</small></div><span>→</span></button>)}</div></section></div>}
 else if(tab==="forecast"&&dataset){body=<ForecastView result={result} dataset={dataset} token={token} datasetName={dataset.name}/>}
 else {body=<ArtifactViews tab={tab} result={result} datasetName={dataset?.name} onSelect={setSelected}/>}
 return <div className="workspace"><aside className="history"><div className="brand"><b>G</b><span>Gamur</span></div><button className="new" onClick={()=>{setResult(null);setDataset(null);setChat([]);setPrompt("");setError("");setTab("analyze")}}>＋ New analysis</button><div className="historyTitle">RECENT ANALYSES</div>{history.length?history.map(h=><div className="historyRow" key={h.id}><button className="historyItem" onClick={()=>openHistory(h)}><span className="historyDot"/><div><b>{h.title||"Analysis"}</b><small>{new Date(h.created_at).toLocaleDateString()}</small></div></button><button className="renameBtn" onClick={()=>{setRenameId(h.id);setRenameValue(h.title||"")}}>•••</button><button className="deleteBtn" onClick={()=>setDeleteId(h.id)}>×</button></div>):<p className="empty">Your saved analyses will appear here.</p>}</aside><section className="work"><header className="appHeader"><nav className="tabs">{tabs.map(([id,label])=><button key={id} className={tab===id?"active":""} onClick={()=>setTab(id)}>{label}</button>)}</nav><div className="profileWrap"><button className="profile" onClick={()=>setProfileOpen(v=>!v)}>G</button>{profileOpen&&<div className="profileMenu"><b>Gamur account</b><span>Signed in</span><a href="/profile">Profile</a><a href="/settings">Settings</a><button onClick={()=>sb?.auth.signOut()}>Sign out</button></div>}</div></header><main className="content"><input ref={inputRef} hidden type="file" accept=".csv,.xlsx" onChange={e=>{const f=e.target.files?.[0];if(f&&/\.(csv|xlsx)$/i.test(f.name))void run(f);else if(f)setError("Use CSV or XLSX files.")}}/><div className="topline"><div><span className="eyebrow">DATA INTELLIGENCE</span><h1>{dataset?.name||"Bring data to life."}</h1><p>{dataset?`${a?.rows?.toLocaleString()||"—"} observations · ${a?.columns||"—"} fields · completed investigation ready to reuse across every workspace.`:"Upload an Excel or CSV file. Gamur investigates it automatically."}</p></div><select value={project?.id||""} onChange={e=>{setProject(projects.find(p=>p.id===e.target.value)||null);setDataset(null);setResult(null);setChat([])}}><option value="">Project</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div>{error&&<div className="error">{error}</div>}{body}{chat.length>0&&<section className="chatPanel"><div className="chatHeader"><span>{askLabels[tab]}</span><small>Context: completed analysis</small></div>{chat.map((m,i)=><div className={m.role==="user"?"chatUser":"chatAssistant"} key={i}><b>{m.role==="user"?"You":"Gamur"}</b><p>{m.text}</p></div>)}</section>}</main><footer className="composer"><div className="composerLabel">{askLabels[tab]}</div><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();void sendChat()}}} placeholder={result?`Ask Gamur about ${tab==="analyze"?"your analysis":tab==="visualize"?"visuals and statistics":tab}:"Describe an analysis goal or upload your data…"}/><button onClick={()=>inputRef.current?.click()}>Attach</button><button className="run" disabled={busy} onClick={()=>prompt.trim()?void sendChat():void run()}>{busy?"Working…":prompt.trim()?"Ask →":"Analyze data"}</button></footer></section>{selected&&<div className="modalShade" onClick={()=>setSelected(null)}><article className="detailPanel" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setSelected(null)}>×</button><span className="sectionTag">EVIDENCE DETAIL</span><h2>{selected.question||`${selected.x||"Finding"} ${selected.y?`↔ ${selected.y}`:""}`}</h2>{selected.r!==undefined&&<div className="detailMetric"><b>r = {Number(selected.r).toFixed(3)}</b><span>{selected.strength||"Measured association"}</span></div>}<p>{selected.answer||`The measured relationship is ${Number(selected.r).toFixed(3)}. Association in observed data does not by itself establish causation.`}</p></article></div>}{renameId&&<div className="modalShade"><article className="renamePanel"><h3>Rename analysis</h3><input autoFocus value={renameValue} onChange={e=>setRenameValue(e.target.value)}/><div><button onClick={()=>setRenameId(null)}>Cancel</button><button className="run" onClick={()=>void renameSaved()}>Save name</button></div></article></div>}{deleteId&&<div className="modalShade"><article className="renamePanel"><h3>Delete this analysis?</h3><p>This removes the saved history entry. The uploaded dataset is not deleted.</p><div><button onClick={()=>setDeleteId(null)}>Cancel</button><button className="run danger" onClick={()=>void deleteSaved()}>Delete</button></div></article></div>}</div>;
}
